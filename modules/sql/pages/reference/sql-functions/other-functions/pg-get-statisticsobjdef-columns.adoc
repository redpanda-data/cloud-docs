= pg_get_statisticsobjdef_columns()
:description: The link:https://www.postgresql.org/docs/current/functions-info.html#FUNCTIONS-INFO-CATALOG[pg_get_statisticsobjdef_columns()] is a system catalog inf

The link:https://www.postgresql.org/docs/current/functions-info.html++#++FUNCTIONS-INFO-CATALOG[pg++_++get++_++statisticsobjdef++_++columns()] is a system catalog information function that retrieves information about the columns associated with an extended statistics object.

=== Syntax

The syntax for the `pg++_++get++_++statisticsobjdef++_++columns()` function is as follows:

....
pg_get_statisticsobjdef_columns() → NULL
....

=== Restrictions

* This function always returns `NULL` if there are no parameters specified

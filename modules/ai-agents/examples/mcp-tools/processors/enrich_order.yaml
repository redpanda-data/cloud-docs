# Enrich order with customer data
# Example of processor tool with HTTP call
# tag::complete[]
label: enrich-order

processors:
  - http:
      url: "https://api.example.com/lookup"
      verb: GET

meta:
  mcp:
    enabled: true
    description: "Enrich order with customer data"
# end::complete[]

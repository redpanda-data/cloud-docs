# Using secrets in tool configurations
# Reference secrets with ${secrets.SECRET_NAME} syntax

# tag::example[]
http:
  url: "https://api.example.com/data"
  headers:
    Authorization: "Bearer ${secrets.API_TOKEN}"

sql_select:
  driver: postgres
  dsn: "${secrets.DATABASE_URL}"
  table: customers
# end::example[]
